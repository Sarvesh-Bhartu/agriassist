[build]
builder = "nixpacks"

[phases.setup]
nixPkgs = ["...", "libGL", "glib", "stdenv.cc.cc.lib"]
nixLibs = ["libGL", "glib", "stdenv.cc.cc.lib"]

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
